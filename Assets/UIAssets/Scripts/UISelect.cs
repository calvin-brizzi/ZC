using UnityEngine;
using System.Collections;

public class UISelect : MonoBehaviour {
	public Canvas canv;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		//GameObject test = canv.transform.FindChild ("Grunt").gameObject;
		//test.SetActive (false);
	}
}
